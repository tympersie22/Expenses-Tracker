# Local restore drill — 24 September 2026

- Source: dedicated local `money_well_test` PostgreSQL database after all migrations and integration tests
- Format: `pg_dump` custom format, owner and privileges excluded
- Destination: isolated `expenses_tracker_restore_drill` database
- Validation: archive listing passed, destination schema was recreated, `pg_restore` completed without error, and the expected User, Movement, and AuditEvent tables were queryable
- Result: passed
- Recovery time: under one minute on the local workstation
- Recovery point: dump start time

The integration suite cleans up its test users, so restored user, movement, and audit-event counts were zero. This proves the repository scripts and schema can restore an empty post-test database. A production-like populated drill and managed-provider point-in-time recovery test remain required before public launch.
